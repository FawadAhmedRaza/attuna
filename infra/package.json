{
  "name": "@attuna/infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK infrastructure for Attuna.",
  "bin": {
    "attuna-infra": "bin/attuna.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "cdk": "cdk",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "clean": "rm -rf cdk.out dist"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.165.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "aws-cdk": "^2.165.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
